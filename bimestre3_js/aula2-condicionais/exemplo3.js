const media=6

if (media > 5) {
    console.log(`Você foi aprovado(a) com a média ${media}. `)
} else {
    console.log(`Você está reprovado(a) com a média ${media}.Você precisa estudar mais` )
}